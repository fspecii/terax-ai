{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dialog",
  "description": "Native folder picker for opening a space/workspace root, including external drives.",
  "windows": [
    "main"
  ],
  "permissions": [
    "dialog:allow-open"
  ]
}
